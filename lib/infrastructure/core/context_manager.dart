import 'package:flutter/material.dart';

///ContextManager: Class that manages the context of the application.
class ContextManager {
  late BuildContext context;
  late Size screenSize;
  ContextManager();
}
